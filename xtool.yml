version: 1

# Keep in sync with the Xcode project's PRODUCT_BUNDLE_IDENTIFIER.
bundleID: com.fdkevin.imagebackup

# xtool does not expand Xcode build-setting placeholders in this file.
infoPath: ImageBackup-Info.plist

resources:
  - app/PrivacyInfo.xcprivacy

# Replace this 1024x1024 placeholder with final art at the same path.
iconPath: Artwork/AppIcon.png
